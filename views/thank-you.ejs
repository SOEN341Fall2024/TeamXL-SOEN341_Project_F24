<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You</title>
    <!-- Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
            margin: 0;
        }
        .thank-you-container {
            background-color: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.15);
            text-align: center;
            max-width: 500px;
            width: 90%;
            animation: fadeIn 0.5s ease-in-out;
        }
        .thank-you-container h1 {
            color: #28a745;
            margin-bottom: 20px;
            font-size: 2rem;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }
        .thank-you-container h1 i {
            font-size: 2.5rem;
        }
        .thank-you-container p {
            margin-bottom: 30px;
            font-size: 1.1rem;
            color: #6c757d;
        }
        .thank-you-container .btn {
            font-size: 1rem;
            padding: 10px 20px;
            transition: all 0.3s ease;
        }
        .thank-you-container .btn:hover {
            transform: scale(1.05);
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .countdown {
            font-weight: bold;
            color: #28a745;
        }
    </style>
</head>
<body>

    <div class="thank-you-container">
        <h1><i class="bi bi-check-circle-fill"></i> Submission Successful!</h1>
        <p>Thank you for submitting your evaluation. You’ll be redirected to the dashboard in <span class="countdown" id="countdown">5</span> seconds.</p>
        <a href="/student-dashboard" class="btn btn-primary">Go Back to Dashboard</a>
    </div>

    <!-- JavaScript for countdown and redirect -->
    <script>
        let countdownNumber = 5;
        const countdownElement = document.getElementById("countdown");

        const countdownInterval = setInterval(() => {
            countdownNumber--;
            countdownElement.textContent = countdownNumber;
            if (countdownNumber <= 0) {
                clearInterval(countdownInterval);
                window.location.href = "/student-dashboard";
            }
        }, 1000);
    </script>

    <!-- Bootstrap Icons and JS for interactions -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
