<!DOCTYPE html>
<html>
<head>
    <title>Chatroom</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Include Bootstrap Icons (optional) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        .chat-container {
            max-width: 800px;
            margin: 40px auto;
            height: 80vh;
            border: 1px solid #ccc;
            display: flex;
            flex-direction: column;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .chat-header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }
        .chat-messages {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            background-color: #f8f9fa;
            border-top: 1px solid #ddd;
            border-bottom: 1px solid #ddd;
        }
        .chat-input {
            padding: 15px;
            border-top: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <div class="container py-4 my-5">
        <div class="chat-container rounded-3 shadow-sm">
            <div class="chat-header">
                <h1 class="h3 text-white">Chatroom</h1>
            </div>
            <div class="chat-messages">
                <div class="d-flex mb-3">
                    <div class="me-2">
                        <i class="bi bi-person-circle text-primary" style="font-size: 1.5rem;"></i>
                    </div>
                    <div>
                        <h5 class="mb-1">User1</h5>
                        <p class="text-muted mb-0">Hello everyone!</p>
                    </div>
                </div>
                <div class="d-flex justify-content-end mb-3">
                    <div class="text-end">
                        <h5 class="mb-1">You</h5>
                        <p class="text-muted mb-0">Hi there!</p>
                    </div>
                    <div class="ms-2">
                        <i class="bi bi-person-circle text-primary" style="font-size: 1.5rem;"></i>
                    </div>
                </div>
            </div>
            <div class="chat-input d-flex">
                <input type="text" class="form-control me-2" placeholder="Type a message...">
                <button class="btn btn-outline-primary rounded-pill">Send</button>
            </div>
        </div>
    </div>

    <!-- Include Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
