<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Confirmation</title>
    <!-- Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
            margin: 0;
        }
        .confirmation-container {
            background-color: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
            text-align: center;
            animation: fadeIn 0.5s ease-in-out;
        }
        .confirmation-container h1 {
            color: #007bff;
            margin-bottom: 20px;
            font-size: 1.8rem;
        }
        .review-summary {
            background-color: #f9f9f9;
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 25px;
            text-align: left;
            transition: background-color 0.3s ease;
            border: 1px solid #e0e0e0;
        }
        .review-summary:hover {
            background-color: #f1f1f1;
        }
        .review-summary h5 {
            color: #343a40;
            margin-bottom: 10px;
            font-size: 1.2rem;
            font-weight: bold;
        }
        .review-summary p {
            margin-bottom: 5px;
            color: #555;
            font-size: 1.05rem;
        }
        .progress-bar {
            height: 8px;
            border-radius: 5px;
            background-color: #007bff;
            margin: 10px 0 20px;
            transition: width 0.4s ease;
            width: 80%;
            box-shadow: inset 0px 2px 4px rgba(0, 0, 0, 0.2);
        }
        .btn-group {
            display: flex;
            gap: 12px;
            justify-content: center;
        }
        .btn-group .btn {
            flex: 1;
            font-size: 1rem;
            padding: 12px;
            border-radius: 30px; /* Fully rounded edges */
            transition: all 0.3s ease;
        }
        .btn-group .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-group .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .btn-group .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-group .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
        }
        /* Fade-in animation */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>
<body>

    <div class="confirmation-container">
        <!-- Progress Indicator -->
        <div class="progress-bar"></div>
        
        <h1>Review Your Submission</h1>
        <p>Please review your evaluation summary below. You can confirm your submission, edit any details, or cancel if necessary.</p>

        <!-- Review Summary Section -->
        <div class="review-summary">
            <h5>Evaluation Summary</h5>
            <p><strong>Cooperation Rating:</strong> <%= cooperation %> / 5</p>
            <p><strong>Conceptual Contribution Rating:</strong> <%= conceptual_contribution %> / 5</p>
            <p><strong>Practical Contribution Rating:</strong> <%= practical_contribution %> / 5</p>
            <p><strong>Work Ethic Rating:</strong> <%= work_ethic %> / 5</p>
            <h5>Overall Comments</h5>
            <p><%= additional_comments %></p>
        </div> 

        <!-- Button Group -->
        <div class="btn-group">
            <!-- Button to go back to edit the evaluation -->
            <a href="/edit-evaluation" class="btn btn-secondary">Edit Evaluation</a>
            <!-- Button to confirm the submission without confirmation dialog -->
            <form action="/thank-you" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-primary">Confirm Submission</button>
            </form>
            <!-- Cancel Button to discard evaluation -->
            <a href="/cancel-review" class="btn btn-danger">Cancel</a>
        </div>
    </div>

    <!-- Bootstrap JS for interactions -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script> 
</body>
</html>
